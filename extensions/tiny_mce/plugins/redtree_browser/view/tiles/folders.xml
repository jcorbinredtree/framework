<?xml version="1.0" encoding="UTF-8"?>
<template xmlns:c = "class://CoreTag">
    <div id = "dms-folder-control" />
    <script type = "text/javascript">    
        function onLoadChildren(tree) {
            <c:forEach list = "${this.folders}" var = "folder">
                new DMSFolder({
                    label : "${=folder.name}", 
                    parent : tree.getRoot(),
                    id : ${=folder.id}
                });
            </c:forEach>
        }
    </script>
</template>