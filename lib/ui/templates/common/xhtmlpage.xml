<template
  xmlns:c="class://CoreTag"
  xmlns:t="class://ThemeTag">

<c:doctype type="xhtml 1.0" />
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <title><c:out value="${page.formatTitle()}" /></title>
    <!-- FIXME links, scripts, yada -->
  </head>
  <body>
    <t:pageBuffer area="content" />

    <!--
      Output warnings and errors, this only generates output if no inner
      template already did this, or if there's been errors generated since
      then.
    -->
    <t:warnings />
    <t:notices />
  </body>
</html>
</template>
