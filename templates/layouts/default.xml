<template
  xmlns:core="class://CoreTag"
  xmlns:ui="class://UiTag"
  core:extends="${this.pageTemplate}">

<ui:addAssets>
  <stylesheet href="css/layouts/default.css" />
</ui:addAssets>

<div id="main-container">
  <div id="left">
    <!-- TODO implement navigation
    <div id= "nav">
      <ul>
        <core:forEach list="${page.getLeftNavigation()}" var="item">
          <li><a href="${=item.href}">${=item.label}</a></li>
        </core:forEach>
      </ul>
    </div>
    -->
    <ui:pageBuffer area="left" />
  </div>

  <div id="center">
    <ui:pageBuffer area="content" />
    <ui:warnings />
    <ui:notices />
  </div>
</div>

</template>
