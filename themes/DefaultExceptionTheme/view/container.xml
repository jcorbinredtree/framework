<template xmlns:c = "class://CoreTag" xmlns:t = "class://ThemeTag" xmlns:f = "class://FrameworkTag">
    <c:doctype type="xhtml 1.1" />
    <html>
            <head>
                    <t:head />
                    <style type = "text/css">
                    body {
                    	background-color: #A8CFB1;
                    }
                    </style>
            </head>
            <body>
                    <t:warnings />
                    <t:notices />

					<div id = "main">
                        <h1>@{=get_class(${this.layout.content})}: ${=this.layout.content.getMessage()}</h1>
						<ul>
                            <li>${=this.layout.content.getFile()} (${=this.layout.content.getLine()})</li>      
							<c:forEach list = "${this.layout.content.getTrace()}" var = "frame">
                                <c:if test = "@{array_key_exists('file', ${frame})}">
                                    <li>${=frame['file']} (${=frame['line']})</li>
                                </c:if>
							</c:forEach>
						</ul>							
					</div>
            </body>
    </html>
</template>